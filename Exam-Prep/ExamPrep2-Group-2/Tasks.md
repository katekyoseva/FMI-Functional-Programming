# Подготовка за Тест 2

## Обхват: Седмици 7 - 11

| Тема              | Седмица | Ключови концепции                                            |
| ----------------- | ------- | ------------------------------------------------------------ |
| ADTs              | 7       | `data`, конструктори, records, `deriving`                    |
| Maybe/Either      | 8       | `Maybe a`, `Either e a`, `case`, `fromMaybe`, `catMaybes`    |
| Типови класове    | 9       | `class`, `instance`, `Eq`, `Ord`, `Show`, собствени          |
| Монади и do       | 10      | `>>=`, `return`, `do`-нотация, Maybe/Either/List като монади |
| IO и сериализация | 11      | `IO`, `getLine`, `putStrLn`, `readFile`, `writeFile`, CSV    |

---

## Задача 1: ADT + Pattern matching

Дефинирайте тип `Expr` за аритметични изрази с числа, събиране, умножение и отрицание. Напишете `eval :: Expr -> Double` и `prettyPrint :: Expr -> String`.

---

## Задача 2: Maybe верижни операции

Напишете `safeOps :: [Int] -> Maybe Int`, която:

1. Взема първия елемент (безопасно)
2. Проверява дали е > 0 (иначе Nothing)
3. Дели 100 на него (безопасно)

Решете с `do`-нотация.

---

## Задача 3: Типови класове

Дефинирайте типов клас `Serializable` с метод `serialize :: a -> String` и `deserialize :: String -> Maybe a`.

Напишете инстанции за `Int`, `Bool` и `[a]` (ако `a` е `Serializable`).

---

## Задача 4: Binary Tree + Foldable

Дефинирайте `Tree a` и напишете:

- `treeToList :: Tree a -> [a]` (inorder)
- `treeFromList :: Ord a => [a] -> Tree a` (BST)
- `treeFoldr :: (a -> b -> b) -> b -> Tree a -> b`

---

## Задача 5: Either + do-нотация

Напишете мини-калкулатор, който парсва низ от вида `"3 + 4"` и пресмята резултата, като всяка стъпка може да върне `Either String Double`.

---

## Задача 6: IO задача

Опишете (без да изпълнявате) програма, която:

1. Чете CSV файл с колони `Име,Оценка`
2. Филтрира студентите с оценка ≥ 4.0
3. Записва резултата в нов файл

---

## Задача 7: Комплексна

Дефинирайте `data JSON = ...` и напишете:

- `jsonGet :: String -> JSON -> Maybe JSON`
- `jsonToString :: JSON -> String`
- `instance Eq JSON`

# Haskell — Задачи за подготовка

---

## Задача 1 — Основи и функции от по-висок ред

**а)** Имплементирайте функция `myFoldr :: (a -> b -> b) -> b -> [a] -> b`, без да използвате `foldr` или `foldl`.

---

**б)** Имплементирайте функция `myZipWith :: (a -> b -> c) -> [a] -> [b] -> [c]`, без да използвате вградени функции освен pattern matching и рекурсия.

---

**в)** Напишете функция `applyN :: Int -> (a -> a) -> a -> a`, която прилага дадена функция `n` пъти върху начална стойност.

Пример: `applyN 3 (+10) 0 == 30`

---

**г)** Напишете функция `myIterate :: (a -> a) -> a -> [a]`, която генерира безкраен списък, като прилага функцията последователно.

Пример: `take 5 (myIterate (*2) 1) == [1,2,4,8,16]`

---

**д)** Напишете функция `composeAll :: [a -> a] -> a -> a`, която прилага всички функции от списъка последователно **отдясно наляво** (като `(.)`).

Пример: `composeAll [(+1), (*2), (+3)] 4 == 15` _(т.е. ((4+3)\*2)+1)_

---

**е)** Напишете функция `myUnfoldr :: (b -> Maybe (a, b)) -> b -> [a]`, която генерира списък от начална seed стойност, докато функцията върне `Nothing`.

Пример: `myUnfoldr (\n -> if n == 0 then Nothing else Just (n, n-1)) 5 == [5,4,3,2,1]`

---

## Задача 2 — Типови класове

**а)** Дефинирайте тип `Solid`, представящ триизмерни тела: куб (страна), сфера (радиус) и цилиндър (радиус и височина).

---

**б)** Направете `Solid` инстанция на `Eq` и `Show`, **без да използвате `deriving`**.

---

**в)** Напишете функция `volume :: Solid -> Double`, която пресмята обема на тялото.

> **Напомняне:** $V_{\text{куб}} = a^3$, $\quad V_{\text{сфера}} = \tfrac{4}{3}\pi r^3$, $\quad V_{\text{цилиндър}} = \pi r^2 h$

---

**г)** Дефинирайте тип клас `Describable` с метод `describe :: a -> String`, който връща текстово описание. Направете `Solid` инстанция на `Describable`.

---

**д)** Направете `Solid` инстанция на `Ord`, наредена по обем — тялото с по-малък обем е по-малко.

---

## Задача 3 — Списъци и операции

**а)** Напишете функция `splitWhen :: (a -> Bool) -> [a] -> [[a]]`, която разбива списък на подсписъци при всеки елемент, за който предикатът е `True`. Разделителите **не** се включват в резултата.

Пример: `splitWhen (== 0) [1,2,0,3,4,0,5] == [[1,2],[3,4],[5]]`

---

**б)** Напишете функция `windows :: Int -> [a] -> [[a]]`, която генерира всички последователни подсписъци с фиксирана дължина `n`.

Пример: `windows 3 [1..5] == [[1,2,3],[2,3,4],[3,4,5]]`

---

**в)** Напишете функция `myTranspose :: [[a]] -> [[a]]`, която транспонира матрица (списък от списъци). Различни редове може да имат различна дължина — резултатът трябва да обхваща до края на най-дългия ред.

Пример: `myTranspose [[1,2,3],[4,5,6]] == [[1,4],[2,5],[3,6]]`

---

**г)** Напишете функция `frequencies :: (Eq a) => [a] -> [(a, Int)]`, която приема списък и връща наредени двойки `(елемент, брой срещания)`, **сортирани по брой срещания в намаляващ ред**.

---

**д)** Напишете функции `rleEncode :: (Eq a) => [a] -> [(Int, a)]` и `rleDecode :: [(Int, a)] -> [a]`, реализиращи **run-length encoding**.

Пример: `rleEncode "aaabbc" == [(3,'a'),(2,'b'),(1,'c')]`  
Пример: `rleDecode [(3,'a'),(2,'b'),(1,'c')] == "aaabbc"`

---

**е)** Дефинирайте тип `MinHeap a`, представящ **минимална пирамида** като двоично дърво. Напишете функции за вмъкване на елемент и извличане на минималния елемент.

---

## Задача 4 — N-арни дървета

**а)** Дефинирайте тип за **N-арно дърво** (`NaryTree`) — дърво, в което всеки възел може да има произволен брой деца.

---

**б)** Напишете функция `treeMap`, която работи като `map` върху N-арно дърво — прилага подадена функция към всеки връх.

---

**в)** Напишете функция, която приема N-арно дърво с върхове от тип `(String, Int)` и връща ново дърво, в чиито върхове е записана само `String` компонентата.

---

**г)** Напишете функция, която приема N-арно дърво с върхове от тип `(String, Int)` и връща **сумата** на всички `Int` стойности в дървото.

> **Забележка:** За решения, които първо превръщат дървото в списък, се дават само половината точки. _(Бонус: напишете функция, която превръща N-арно дърво в списък.)_

---

**д)** Напишете функция, която приема N-арно дърво с върхове от тип `Either String Int` и връща ново дърво, в което всеки връх, съдържащ `Int` стойност, е **увеличен с 1**.

---

**е)** Направете `NaryTree` инстанция на `Foldable`. Използвайте я, за да напишете функция, която пресмята сбора на всички елементи в дърво от числа, **без ръчна рекурсия**.

---

**ж)** Напишете функция `treeHeight`, която приема N-арно дърво и връща височината му.

---

**з)** Напишете функция `nodesAtDepth :: Int -> NaryTree a -> [a]`, която връща стойностите на всички възли, намиращи се на дълбочина точно `n` от корена (коренът е на дълбочина 0).

Пример: `nodesAtDepth 0 tree == [стойността на корена]`

---

**и)** Напишете функция `parentOf :: Eq a => a -> NaryTree a -> Maybe a`, която приема стойност `x` и дърво и връща стойността на **родителя** на първия намерен възел с тази стойност. Ако `x` е в корена или не съществува, върнете `Nothing`.

---

**й)** Напишете функция `grandparentOf :: Eq a => a -> NaryTree a -> Maybe a`, която връща стойността на **прародителя** (родителя на родителя) на първия намерен възел с дадената стойност.

---

**к)** Напишете функция `greatGrandchildren :: NaryTree a -> [a]`, която връща стойностите на всички **правнуци** на корена — т.е. на дълбочина точно 3.

---

**л)** Напишете функция `pathTo :: Eq a => a -> NaryTree a -> Maybe [a]`, която връща пътя от **корена до** (включително) първия намерен възел с дадената стойност.

Пример: За дърво `Node 1 [Node 2 [Node 4 []], Node 3 []]` — `pathTo 4 tree == Just [1, 2, 4]`

---

## Задача 5 — Работни задачи и тикет номера

**а)** Дефинирайте тип `Deadline`, който представлява краен срок чрез три числа — ден, месец и година.

---

**б)** Дефинирайте тип `WorkItem`, който описва работна задача със следните данни:

- **Заглавие** (`String`)
- **Тикет номер** (`Maybe String`)
- **Краен срок** (`Deadline`)
- **Подзадачи** — списък от работни задачи от тип `WorkItem` (може да е празен)

> **Формат на тикет номер:** `(2 главни латински букви = код на проект)(4 цифри = година)(4 цифри = пореден номер)` — общо **10 символа**.  
> Пример: `"AB20260001"` — проект AB, 2026, задача 0001.  
> За **критични** задачи поредният номер е между `5000` и `9999`.  
> За **обикновени** задачи поредният номер е между `0001` и `4999`.

---

**в)** Напишете функция, която приема заглавие, списък от подзадачи, краен срок и тикет номер и връща нов обект от тип `WorkItem`. Функцията трябва да **валидира тикет номера** и при невалиден да връща грешка чрез `error`.

---

**в2)** _(Бонус)_ Напишете вариант на горната функция, който вместо `error` връща `Either String WorkItem`, където `Left` съдържа описание на грешката.

---

**г)** Напишете функция `allSubtasks :: WorkItem -> [WorkItem]`, която приема `WorkItem` и връща списък от **всичките му подзадачи** на всички нива.

---

**д)** Напишете функция `parentTask :: Eq WorkItem => WorkItem -> WorkItem -> Maybe WorkItem`, която приема **дърво** (корен) и **целева задача** и връща директния родител на целевата задача в дървото, или `Nothing` ако е коренът или не се среща.

> **Упътване:** Трябва да направите `WorkItem` инстанция на `Eq`. Помислете кое поле е подходящ уникален ключ.

---

**е)** Напишете функция `grandparentTask :: Eq WorkItem => WorkItem -> WorkItem -> Maybe WorkItem`, която използва резултата от предходната подточка.

---

**ж)** Напишете функция `greatGrandSubtasks :: WorkItem -> [WorkItem]`, която връща всички подзадачи на дълбочина **точно 3** от дадения елемент.

---

**з)** Напишете функция, която приема списък от `WorkItem` и **присвоява валиден уникален тикет номер** на всяка задача и на всичките й подзадачи. Поредният номер в края започва от `0001` и нараства с всяко ново присвояване. Приемете, че всеки тикет номер, който вече има стойност, е валиден.

> **Упътване:** Първо напишете функция, която приема един `WorkItem` и присвоява тикет номера само на подзадачите му.

> **Бонус:** Гарантирайте уникалност и спрямо вече съществуващите тикет номера в системата.

---

**и)** Напишете функция, която приема списък от `WorkItem` и връща списък от всички **уникални** работни задачи в системата (включително всички подзадачи).

---

**й)** Дефинирайте тип `Project`, който представлява проект с **наименование** (`String`), **двубуквен код** (`String`) и **списък от `WorkItem`**.

---

**к)** Напишете функция, която приема стойност от тип `Project` и връща списък от наредени двойки `(задача, [листни подзадачи])` — за всяка задача намерете нейните крайни подзадачи (тези без собствени подзадачи).

---

**л)** Напишете функция, която приема `WorkItem` и число `n` и присвоява валидни уникални тикет номера на задачата и нейните подзадачи, като поредният номер започва от `n`.

---

## Задача 6 — Maybe, Either и IO

**а)** Напишете функция `safeSqrt :: Double -> Maybe Double`, която връща `Nothing` за отрицателни числа и `Just` корена в противен случай.

---

**б)** Напишете функции `safeHead :: [a] -> Maybe a` и `safeLast :: [a] -> Maybe a`, които връщат `Nothing` за празен списък.

---

**в)** Напишете функция `chainMaybe :: [a -> Maybe a] -> a -> Maybe a`, която прилага поредица от функции, всяка връщаща `Maybe`, и спира при първото `Nothing`.

Пример: `chainMaybe [Just . (+1), \x -> if x > 3 then Nothing else Just x] 2 == Just 3`  
Пример: `chainMaybe [Just . (+1), \x -> if x > 3 then Nothing else Just x] 3 == Nothing`

---

**г)** Напишете функция `validateAll :: [a -> Either String ()] -> a -> Either String a`, която прилага поредица от валидации върху стойност. При първата грешка връща `Left` с описанието ѝ; ако всички минат — връща `Right` стойността.

---

**д)** Напишете функция, която чете числа от терминала (по едно на ред) до въвеждане на `"0"` и отпечатва тяхната **сума** и **средно аритметично**.

---

**е)** Напишете функция, която приема `WorkItem`, чете число от терминала и присвоява валидни уникални тикет номера на задачата и нейните подзадачи, **започвайки поредния номер от прочетеното число**.

---

**ж)** Напишете функция `parseCSV :: String -> [String]`, която разбива низ, разделен със запетаи, на списък от низове (без водещи/крайни интервали за всеки елемент).

Пример: `parseCSV "Alice, 25, Sofia" == ["Alice","25","Sofia"]`

---

**з)** Напишете програма, която чете от терминала думи (по една на ред, до `"СТОП"`) и отпечатва:

- броя на въведените думи
- най-дългата въведена дума
- всички думи с дължина, по-голяма от средната дължина